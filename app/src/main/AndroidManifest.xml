<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CurioApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Share-sheet capture: any tweet / URL / text shared into Curio -->
            <intent-filter android:label="Save to Curio">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- OAuth PKCE redirect URI capture intent filter -->
            <intent-filter android:label="Curio OAuth Callback">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="curio-oauth" android:host="callback" />
            </intent-filter>
        </activity>

        <service
            android:name=".background.BookmarkSweeperService"
            android:exported="false" />

        <!-- AppFunctions: platform-side service for Android 16+ (API 36+). The
             enablePlatformAppFunctionService boolean is provided by the appfunctions library's
             own bools.xml (false by default, true on values-v36). -->
        <service
            android:name="androidx.appfunctions.PlatformAppFunctionService"
            android:permission="android.permission.BIND_APP_FUNCTION_SERVICE"
            android:exported="true"
            android:enabled="@bool/enablePlatformAppFunctionService"
            tools:targetApi="36">
            <property
                android:name="android.app.appfunctions.v2"
                android:value="app_functions_v2.xml" />
            <intent-filter>
                <action android:name="android.app.appfunctions.AppFunctionService" />
            </intent-filter>
        </service>

        <!-- AppFunctions: extension-library backport for pre-API 36 devices with AppSearch
             extensions. The enableExtensionAppFunctionService boolean is true by default and
             switched off on values-v36 by the appfunctions library. -->
        <service
            android:name="androidx.appfunctions.ExtensionAppFunctionService"
            android:permission="android.permission.BIND_APP_FUNCTION_SERVICE"
            android:exported="true"
            android:enabled="@bool/enableExtensionAppFunctionService">
            <property
                android:name="android.app.appfunctions.v2"
                android:value="app_functions_v2.xml" />
            <intent-filter>
                <action android:name="android.app.appfunctions.AppFunctionService" />
            </intent-filter>
        </service>

        <!-- App-level description for AI agents discovering Curio's AppFunctions. -->
        <property
            android:name="android.app.appfunctions.app_metadata"
            android:resource="@xml/app_metadata" />
    </application>

</manifest>
